const name = 'Ariana'
const concatenation = 'Hello ' + name + '!'
const template = `
Hello
${name}!
`
console.log(concatenation, template)

console.log(`1 + 1 = ${1 + 1}`)

const up = text => text.toUpperCase()
console.log(`Yo... ${up('caution')}!`)